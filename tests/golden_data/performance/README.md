# Performance Dataset
**Purpose:** This dataset contains bloated extensions with thousands of files or massive `manifest.json` configurations. It is utilized by `cargo bench` to stress-test memory consumption and CPU utilization of the Analysis Pipeline.
