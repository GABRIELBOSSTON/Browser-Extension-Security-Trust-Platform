# MV2 Golden Dataset
**Purpose:** This dataset contains valid Manifest V2 extensions. It is used by Integration Tests to verify legacy parser logic, such as extracting host patterns from the generic `permissions` array and interpreting legacy `background.page` structures.
