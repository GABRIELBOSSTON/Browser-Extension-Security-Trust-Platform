# Broken Dataset
**Purpose:** This dataset contains intentionally corrupted extensions (malformed JSON, broken directory structures, Zip-slip payloads). It is used to verify that the Analysis Pipeline accurately executes its "fail-fast" error propagation logic without triggering fatal process panics.
